The agent runs a canned "# script" then stops on "# complete".
Also covers empty argv as a usage error even with a pipe, hitting
the prompt cap, a done marker with extra trailing newlines, leading
# notes before done, a "# complete" comment after a script (not
done), notes before "# script" (not on sh stdin), and exported
PID / PROMPT_COUNTER / TEMP_FOLDER not reaching the script.
