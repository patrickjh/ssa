The agent runs a canned script, then stops on "# task complete".
Also covers a task on stdin, hitting the prompt cap, a done marker
with extra trailing newlines, leading # notes before done, and a
"# task complete" comment after a script (not done).
