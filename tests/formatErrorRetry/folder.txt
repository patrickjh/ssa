Invalid shell (prose, markdown fences, thinking tags, Gemma thought
channels, syntax errors) and empty replies are not run. A short
format error is appended and the loop continues so the model can
recover. Writes and edits skip fence and think checks so payloads
may contain those bytes.
