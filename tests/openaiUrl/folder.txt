openaiUrl — OPENAI_URL is only required to be set

Curl POSTs the chat-completions body to whatever URL is given.
There is no path-shape check. A query string or a proxy path that
does not end in /chat/completions still reaches the model.
