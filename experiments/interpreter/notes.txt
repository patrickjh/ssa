experiments/interpreter — one SSA_INTERPRETER + # script

Fork of experiments/formatError/ssa. Not product.
maybeNot/runOtherLanguages.txt. No # run / SSA_RUNNER
(that is experiments/runSentinel/).

Live smoke (manual): SSA_NO_ASK=1 SSA_KEEP_TEMP=1, same
URL/model as README, invoke experiments/interpreter/ssa.
Default SSA_INTERPRETER=sh, then SSA_INTERPRETER=python3
if python3 exists.

Questions (from runOtherLanguages.txt):

1. Does the model emit # script on the hot path with
   python3, or format-error until max prompts?
2. Is one-turn python on stdin better than write foo.py
   then python3 foo.py (interpreter still sh)? Did missing
   __file__ matter?
3. Does a tiny non-sh example stop it from emitting ls -la
   or concatenating kinds (mixedReplyKinds.txt)? Error
   turns still reprint the sh-flavored REPLY_SPEC.
4. Write/edit still work when the interpreter is python3
   (COMMAND -c, not piped at python3)?

Harness: 13 fake-curl cases, sh experiments/interpreter/try.sh
(0 failed).
Live: not run yet (needs a real SSA_URL / SSA_MODEL).
