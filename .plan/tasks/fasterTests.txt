fasterTests — make sh tests/runTests.sh cheaper

Why:
The runner already starts each tests/*/*.test.sh in the
background. A full pass is still minutes (61 cases here).
Each agent-loop story pays prompt0, canned curls, and the
always-on review POST. Fake curl is a new sh script per
request. capMessages stories print huge awk blobs. Do not
treat a faster Windows Git-bash fork as a product change.

Do:
Measure first (time sh tests/runTests.sh; note the slow
stories). Then do cheap harness wins only:

- Keep one test per file and parallel start.
- Do not skip prompt0 or review in product ssa to make
  tests faster. Tests that do not care about review can
  keep using add_default_review_reply.
- Fake curl / add_model_reply: fewer processes or fewer
  jq wraps if that is where time goes.
- Do not shrink capMessages blobs below the cap they
  prove. Do not drop SSA_NO_ASK=1 stories. Do not add a
  second test runner.

If the slowness is only this host’s sh spawn, document
that in tests/ or AGENTS.md and stop.

Size:
0 in product ssa unless measure shows a real ssa cost.
Harness and maybe AGENTS.md extra.

Tests:
sh tests/runTests.sh still 0 failed. Wall time drops or
the note says why it will not.
