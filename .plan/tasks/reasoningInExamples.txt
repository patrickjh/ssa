reasoningInExamples — # reasoning: on each spec example

Why:
The intro says put reasoning in # comments, but REPLY_SPEC
examples are bare sentinels. Gemma 4 31B then comments out
the program (`# ls -la`) or concatenates two example blocks
(mixedReplyKinds.txt). experiments/formatError/ssa shows a
`# reasoning: …` line before each action sentinel so notes
and the kind stay distinct. Product spec after
formatErrorInPrompt still has none of those lines.

Do:
Land after scriptSentinel.txt (`# script` is already in the
spec). Copy the formatError fork's example comments into
REPLY_SPEC only. Same string is already the format-error
body. Do not change dispatch, first_action_line, or SYSTEM_PROMPT_INTRO.

Use these comment lines, exact:

  # reasoning: look at the files in the folder
  # script
  ls -la

  # reasoning: write a hello.txt file
  # write file: hello.txt
  hello
  world

  # reasoning: edit the hello.txt file
  # edit file: hello.txt
  (same SEARCH/REPLACE as today)

Leave # complete with no reasoning line (the fork does).
Do not add `# reasoning:` as a sentinel. Do not use
`# comment:`. Do not grow the intro. Do not bake # run.

If scriptSentinel has not landed, stop; this job needs
`# script` in the samples. If noWrites.txt / noEdits.txt
already omitted a kind, omit that example's reasoning
line too.

Related: scriptSentinel.txt (land first).
mixedReplyKinds.txt (examples as copy targets).
formatErrorInPrompt (shared spec; already landed).
experiments/formatError/ssa.

Size:
About 6 lines in `ssa` (3 comment lines plus blanks in
REPLY_SPEC). Tests extra. No behavior change.

Tests:
Fake curl, SSA_NO_ASK=1. formatErrorRepeatsPromptSample
still matches `# write file: hello.txt`. System prompt and
format error both contain `# reasoning: look at the files
in the folder` and `# reasoning: write a hello.txt file`.
Stdout of a format error lacks a reasoning line on the
`# complete` example only as in the fork (the complete
block is still `# complete` alone). Write/edit failures
still lack `Format error`.
