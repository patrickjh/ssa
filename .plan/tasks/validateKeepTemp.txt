validateKeepTemp — SSA_KEEP_TEMP must be 0 or 1

Why:
SSA_NO_ASK is treated as 0|1. SSA_KEEP_TEMP: any value other than
exactly 0 currently keeps the temp folder.

Do:
Validate 0|1 like other flags, or document the broader truthy
behavior. Prefer validate + die with --keep-temp / SSA_KEEP_TEMP
in the hint.

Tests:
SSA_KEEP_TEMP=yes ssa ... should die (if you validate), or a comment
in -h if you keep truthy.
