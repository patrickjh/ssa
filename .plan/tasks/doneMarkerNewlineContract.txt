doneMarkerNewlineContract — trailing newlines vs docs

Why:
agent_is_done uses command substitution, which strips trailing
newlines. A marker followed by blank lines counts as complete.
AGENTS.md once said there was no trimming. Tests already allow it:
tests/completeSimpleTask/doneWithTrailingNewlines.test.sh.

Do:
Keep the tested behavior (trailing newlines ignored). Update AGENTS.md
and -h so they say that explicitly. Do not switch to byte-exact
comparison unless you change the test.

Size:
About 1 line to change in `ssa` (0–2): say in -h that trailing
newlines on # task complete are ignored. No logic change. AGENTS.md
is extra, not in that count.

No new feature.
