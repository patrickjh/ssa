experimentRunSentinel — do not add # run / SSA_RUNNER

Why:
A space-separated SSA_RUNNER list and `# run RUNNER` were
tried so the model could pick sh vs python3 each turn.
That is not the product shape. Extra names in the prompt
are mix risk (mixedReplyKinds.txt). The fork is gone; do
not recreate experiments/ or copy that dispatch into ssa.

Product maybe for other languages is one SSA_INTERPRETER
and `# script` (runOtherLanguages.txt). Dropping
SSA_SANDBOX_COMMAND is dropSandbox.txt. Write and edit
are harness I/O (writeEditInHarness.txt), not listed
`sh -c`.

Do:
Do not add `# run`, SSA_RUNNER, or a per-turn runner menu.
Do not treat this file as a job to rebuild a fork.

Related: maybeNot/runOtherLanguages.txt.
dropSandbox.txt. writeEditInHarness.txt.
maybeNot/requireReplySentinels.txt. mixedReplyKinds.txt.
doNot.txt.

Size:
0.

Tests:
None.
