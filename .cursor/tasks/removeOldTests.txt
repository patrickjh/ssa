removeOldTests — delete the archived test suites

Why:
oldTests/ is not run by tests/runTests.sh. Live coverage lives under
tests/. Keeping the archive invites new cases in the wrong place and
bloats the tree. AGENTS.md already says leave it alone unless the job
is to remake or remove tests; this is that job.

Do:
Delete the oldTests/ directory. Drop it from the AGENTS.md layout and
from the "leave oldTests/ alone" / "archive only" bullets. README
layout should list tests/ only (see readmeLayoutAndTests.txt).

Grep the repo for oldTests and clear leftover mentions in task files
if those tasks are still open (editFileContract, stdinToTaskFile can
drop the "do not add under oldTests/" lines once the folder is gone).

Do not port remaining oldTests cases in the same change unless a
current tests/ story is actually missing. Most archived behaviors
already have live stories.

Tests:
sh tests/runTests.sh still passes. No test should reference oldTests/.
