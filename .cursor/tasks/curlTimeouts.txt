curlTimeouts — HTTP time limits

Why:
A stalled endpoint can hang the run. Slow local 31B prefill needs a
high total timeout; connect should still be finite.

Stale?:
listOfIssues said curl had no timeouts. send_curl_json_request already
uses --connect-timeout 30 --max-time 300 and --retry 4. Retry-After
parsing and httpCode.txt are gone.

Do:
Confirm 300s is enough for slow 31B, or raise --max-time. If retries
stay (see dropCurlRetries.txt), cap any remaining retry sleep. Prefer
constants in ssa, not new flags; callers can wrap curl on PATH.

Tests/docs:
If behavior changes, note it in AGENTS.md Model (curl) section. No
live network test required.
