timeoutSandboxDocs — hung scripts and leftover pipe writers

Why:
A model can start tail -f, or background a child that keeps stdout
open. tee then waits forever. Hard to fix portably while keeping live
streaming.

Do:
Do not add a new flag. Document --sandbox-command wrapping
timeout/timelimit (and a process group if needed) around sh. README
or -h, one short paragraph. Mention background children that inherit
the pipe.

This merges two list items: optional timeout sandbox for Gemma coding,
and "background children that keep output open."
