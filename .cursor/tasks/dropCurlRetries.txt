dropCurlRetries — one curl, die on failure

Why:
Local Gemma does not need HTTP retries. Cloud 429s can be a curl
wrapper earlier on PATH. Unix shape: one request, fail visibly.

Stale?:
--max-http-requests is already gone. send_curl_json_request still
passes --retry 4 --retry-connrefused --retry-max-time 120.

Do:
Drop the retry flags. Die on non-2xx as today (--fail). Update -h /
AGENTS.md if they mention retries.

If you keep retries, curlTimeouts.txt still wants a sleep cap.
