dropSandboxUser — remove --sandbox-user

Why:
Callers can wrap sudo/doas in --sandbox-command. Fits "do not invent
flags for what the caller's shell can do." Shrinks ssa.

Do:
Remove SSA_SANDBOX_USER, --sandbox-user, id checks, run_as_sandbox_user.
Keep --sandbox-command. Update -h, AGENTS.md sandbox table, README
Safety, and any tests.

If this ships, numericSandboxUser.txt is void — delete that task too.
