numericSandboxUser — UID form of --sandbox-user

Why:
id accepts a numeric UID, but sudo -u 1001 may treat 1001 as a login
name. Startup can pass and every model script then fails.

Do:
Resolve name or UID to a canonical login with id -un before execution,
or use the tool-specific UID syntax. Skip this task if
dropSandboxUser.txt ships first; then delete this file.

Tests:
Unix host only: both a login name and a numeric UID. Fake curl,
--no-ask. Do not require sudo in the default Git-bash Windows run
if the story cannot pass there — keep it skippable or Unix-only.
